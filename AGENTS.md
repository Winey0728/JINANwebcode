# 济南水系文化数字人文平台 - 需求拆解文档

## 产品概述

- **产品类型**: 数字人文展示平台（文化宣传类官网）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 文化研究者、游客、学生、对济南泉水文化感兴趣的公众
- **核心价值**: 以数字化手段整合济南泉水、名士、红色、古城、商埠五大文化体系，提供沉浸式文化体验与学术资源
- **界面语言**: 中文（支持中英文切换）
- **主题偏好**: user_specified（中国风、水墨感、水蓝色调，简洁大气）
- **导航模式**: 锚点导航
- **导航布局**: Topbar（顶部固定导航栏）

---

## 页面结构总览

> **说明**：本项目为单页展示型平台，所有板块通过锚点导航访问

**页面文件**: `HomePage.tsx`

| 区块名称 | 锚点 | 区块说明 |
|---------|------|---------|
| 导航栏 | - | 顶部固定导航，含Logo、导航链接、中英文切换、移动端菜单 |
| 首页Hero | `#hero` | 主标题、副标题、两个CTA按钮 |
| 核心亮点 | `#highlights` | 4个核心亮点卡片 |
| 文化板块 | `#culture-sections` | 6个文化板块卡片网格 |
| 平台介绍 | `#about-platform` | 项目背景、研究价值、数据统计 |
| 文化资源 | `#cultural-resources` | 5个标签切换的文化资源展示 |
| AI智能体 | `#ai-agent` | 4个AI功能卡片 |
| 虚拟漫游 | `#virtual-tour` | 三种模式切换（老照片/全景/翻书） |
| 时空地图 | `#spatiotemporal-map` | Leaflet GIS地图，含图层与时期切换 |
| 关于我们 | `#about-us` | 团队介绍、成员、联系表单 |
| 页脚 | - | 品牌信息、导航链接、版权、时间戳 |

---

## 页面布局建议

- **布局模式**: 单页纵向滚动 + 分段式布局 —— 展示型文化平台，用户通过滚动或导航依次浏览各板块
- **视觉重心**: 内容 —— 以文化内容展示为核心，每个板块有独立的视觉焦点（Hero大图、卡片网格、地图等）
- **结果承载区**: 不适用（本平台为内容展示型，非工具生成类）；各交互模块（标签切换、模式切换、地图操作）在本区块内即时反馈

---

## 插件规划

| 插件实例名称 | 基于官方插件 | 业务用途 | 输出模式 | 所属页面 |
|------------|-----------|---------|---------|---------|
| 文化问答智能体 | `ai-text-generate` | 基于济南水系文化知识库，回答用户关于泉水、名士、历史等问题 | stream | 首页（AI智能体区块） |
| 语音讲解合成 | `ai-speech-synthesis` | 将景点介绍、文化解说文本转换为语音播放 | unary | 首页（AI智能体区块、虚拟漫游区块） |
| 路线规划推荐 | `ai-text-generate` | 根据用户兴趣和时间，生成个性化济南文化游览路线 | stream | 首页（AI智能体区块） |

> **说明**：AI智能体板块的"历史复原"功能为静态展示（历史场景对比图），不涉及插件调用。

---

## 导航配置

- **导航布局**: Topbar（顶部固定，滚动时背景变化）
- **导航项**:

| 导航文字 | 锚点 |
|---------|------|
| 首页 | `#hero` |
| 平台介绍 | `#about-platform` |
| 文化资源 | `#cultural-resources` |
| AI智能体 | `#ai-agent` |
| 虚拟漫游 | `#virtual-tour` |
| 时空地图 | `#spatiotemporal-map` |
| 关于我们 | `#about-us` |

- **附加元素**: Logo（济南水系文化 + 水滴图标）、中英文切换按钮、移动端汉堡菜单

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 文化资源数据（泉水、名士、红色、古城、水系） | demo-mock | `src/data/culturalResources.ts` 中定义各分类的景点/人物数据数组 | ✅ 本身就是 mock，含名称、描述、图片URL等字段 |
| 文化问答对话 | real-plugin | capabilityClient.callStream 调文化问答智能体实例，传入用户问题文本，流式输出回答 | 失败提示 (toast "智能问答服务暂不可用") |
| 语音讲解播放 | real-plugin | capabilityClient 调语音讲解合成实例，传入选中的景点介绍文本，返回语音音频 | 失败提示 (toast "语音合成服务暂不可用") |
| 路线规划生成 | real-plugin | capabilityClient.callStream 调路线规划推荐实例，传入用户偏好（兴趣点、时长），流式输出路线方案 | 失败提示 (toast "路线规划服务暂不可用") |
| 地图点位数据（泉水、文化点位、水系） | demo-mock | `src/data/mapPoints.ts` 定义各时期各图层的经纬度点位数组 | ✅ 本身就是 mock |
| 老照片相册数据 | demo-mock | `src/data/photos.ts` 定义古籍文献影像列表（名称、图片URL、描述） | ✅ 本身就是 mock |
| 联系表单提交 | demo-mock | 前端表单校验 + 提交后 toast 成功提示（不实际发送） | ✅ 模拟提交成功 |
| 虚拟漫游全景场景数据 | demo-mock | `src/data/panoramas.ts` 定义全景场景列表（名称、图片URL、热点坐标） | ✅ 本身就是 mock |

> 类型选择 + 兜底约束见上方"数据来源声明方法论"段。插件类行 type=`real-plugin` + mock 兜底=失败提示，符合 COMMON-3 防御要求。

---

## 功能列表

### 导航栏

- **页面目标**: 提供全站导航入口，支持快速跳转各板块
- **功能点**:
  - **锚点平滑滚动**: 点击导航链接平滑滚动到对应板块，URL 更新 hash
  - **滚动效果**: 页面向下滚动超过阈值后，导航栏背景由透明变为实色（毛玻璃效果），高度缩小
  - **中英文切换**: 点击切换按钮切换界面语言（中/英），当前语言高亮
  - **移动端菜单**: 小屏幕下显示汉堡按钮，点击展开/收起全屏菜单，点击菜单项后自动关闭

### Hero 区域

- **页面目标**: 展示平台主题，引导用户探索核心功能
- **功能点**:
  - **主副标题展示**: 水墨风格渐变文字，配合水波纹动效背景
  - **CTA按钮交互**: "探索文化资源"跳转至文化资源板块，"虚拟漫游体验"跳转至虚拟漫游板块
  - **向下滚动提示**: 底部箭头动画，点击平滑滚动到下一板块

### 核心亮点

- **页面目标**: 快速传达平台四大核心优势
- **功能点**:
  - **卡片悬停动效**: 鼠标悬停时卡片上浮、阴影加深、图标变色
  - **滚动进入动画**: 卡片随页面滚动依次淡入上移

### 文化板块

- **页面目标**: 展示六大文化板块概览
- **功能点**:
  - **卡片网格布局**: 6个卡片响应式网格（桌面3×2，平板2×3，移动端1×6）
  - **卡片点击跳转**: 点击卡片跳转至文化资源板块对应标签
  - **背景图叠加效果**: 每个卡片有文化主题背景图，叠加蓝色半透明遮罩，悬停时遮罩变浅

### 平台介绍

- **页面目标**: 说明项目背景、研究价值与数据规模
- **功能点**:
  - **研究价值列表**: 4条价值点带图标，逐条展示
  - **数据统计数字动画**: 4个统计数字（72名泉、200+文化点位、50+历史名人、1000+文献资料）滚动到视口时触发从0到目标值的计数动画

### 文化资源

- **页面目标**: 分类展示济南文化资源详情
- **功能点**:
  - **标签切换**: 5个标签（泉水文化、名士文化、红色文化、古城文化、水系脉络），点击切换内容区，带下划线滑动动画
  - **泉水文化卡片网格**: 趵突泉、黑虎泉、珍珠泉、五龙潭4个图片卡片，含名称、简介、图片
  - **名士文化分组展示**: 古代名士（曾巩、李清照、辛弃疾、张养浩、李攀龙）与近现代名士（老舍）分组，人物头像卡片
  - **占位内容展示**: 红色文化、古城文化、水系脉络显示"内容建设中"占位图 + 文字

### AI 智能体

- **页面目标**: 展示平台AI能力，提供交互入口
- **功能点**:
  - **文化问答**: 点击卡片展开问答对话框，用户输入问题，流式输出AI回答，支持多轮对话
  - **语音讲解**: 选择景点后点击播放，调用语音合成生成讲解音频，带播放进度条和播放/暂停控制
  - **路线规划**: 弹出表单（兴趣偏好、游览时长），提交后生成个性化游览路线方案（含景点、时长、交通建议）
  - **历史复原**: 展示同一地点古今对比图（滑块左右拖动对比），静态交互效果

### 虚拟漫游

- **页面目标**: 提供三种沉浸式漫游体验
- **功能点**:
  - **模式切换**: 顶部三个模式按钮（老照片相册、全景漫游、Flipbook翻书），点击切换对应内容区
  - **老照片轮播**: 横向滚动轮播古籍影像（济南府属总图、大小清河图、省城街巷全图、济南府城图），支持左右箭头、拖拽滑动、点击放大查看
  - **全景漫游模拟**: 场景选择下拉（大明湖、趵突泉、芙蓉街、护城河），360°全景图可鼠标拖拽旋转，热点标注点击弹出介绍，缩放控制按钮
  - **Flipbook翻书**: 画册卡片网格展示，点击某本画册后 iframe 嵌入翻书效果页面（使用第三方翻书演示URL或静态模拟）

### 时空地图

- **页面目标**: GIS地图展示济南水系文化的时空分布
- **功能点**:
  - **Leaflet地图初始化**: 加载高德底图，中心点定位济南市区，限制拖动范围在济南市区边界内
  - **图层切换**: 4个图层按钮（全部图层、泉水分布、文化点位、水系脉络），点击显示/隐藏对应marker和线
  - **时期切换**: 4个时期按钮（当代、明代、清代、民国），切换后地图点位数据对应变化
  - **点位Marker**: 泉水和文化点位用不同图标标记，点击弹出信息框（名称、简介、图片）
  - **一键复位**: 右下角复位按钮，点击后地图回到初始中心和缩放级别
  - **水系脉络展示**: 用折线绘制小清河、大明湖、护城河等水系走向

### 关于我们

- **页面目标**: 介绍项目团队，提供联系渠道
- **功能点**:
  - **团队成员展示**: 技术开发、内容研究、视觉设计三组，每组成员卡片（头像、姓名、角色）
  - **联系表单**: 姓名、邮箱、留言字段，前端校验，提交后显示成功提示 toast，表单重置

### 页脚 & 全局功能

- **页面目标**: 提供底部导航和全局辅助功能
- **功能点**:
  - **返回顶部按钮**: 页面滚动超过一屏后右下角显示，点击平滑滚动到顶部
  - **实时时间戳**: 页脚显示当前日期时间，每秒更新
  - **版权信息**: 品牌Logo + 版权声明
  - **底部导航链接**: 快速跳转各板块的文字链接

---

## 数据共享配置

| 存储键名 | 数据说明 | 使用页面/区块 |
|---------|---------|-------------|
| `__global_jnwater_lang` | 当前界面语言，类型为 `'zh' | 'en'` | 导航栏、所有内容区块 |
| `__global_jnwater_cultureTab` | 文化资源当前激活标签，类型为 `string` | 文化板块、文化资源区块 |

```ts
// 语言包类型定义
interface ILocale {
  nav: {
    home: string;
    about: string;
    resources: string;
    ai: string;
    tour: string;
    map: string;
    contact: string;
  };
  hero: {
    title: string;
    subtitle: string;
    btn1: string;
    btn2: string;
  };
  // ... 其他板块文案
}

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 用户给出完整功能结构与风格关键词，无成品截图或手绘稿，视觉方向由"济南水系文化 + 水墨感 + 水蓝色调"语义自主构建
- **核心情绪 / 应用类型**: 文化数字人文平台 Landing + 内容展示，传递"泉水清润、古城文脉、数字新生"的雅致厚重感
- **独特记忆点**: 以"水墨晕染的水滴涟漪"作为贯穿全站的视觉母题——Hero 背景水纹、卡片 hover 涟漪扩散、分隔线水墨渐变、导航滚动时的水雾玻璃质感

## 2. Art Direction

- **方向名**: 水墨泉韵 · 数字人文
- **Design Style**: Editorial 经典排版 + 水墨东方美学 —— 文化内容需要阅读舒适度与书卷气，数字平台需要现代清晰的信息架构，二者结合形成"古意新说"的气质
- **DNA 参数**: 圆角 subtle (`rounded-md`) / 阴影 subtle 水墨晕染感 (`shadow-sm` + 低透明度蓝青渐变) / 间距 spacious (`gap-6` / `p-8`) / 字体方向：标题衬线宋体气质 + 正文无衬线清晰 / 装饰手法：水波纹底纹、水墨晕染分隔、竖排小标、印章式标签
- **应用类型**: Landing + Content —— 单页叙事滚动，多板块内容展示，交互以浏览与探索为主

## 3. Color System

**色彩关系**: 泉青主色 + 水蓝辅助 + 墨灰文字 + 宣纸白底，整体从"泉水-水墨-宣纸"自然意象中提取，低饱和、高明度差，清雅不刺眼
**配色设计理由**: primary 泉青色承载泉水文化的核心识别与主行动；bg 取宣纸微暖白而非纯白，呼应水墨纸本质感；accent 水蓝用于 hover 与层级提示，模拟水面反光；text 用深墨灰而非纯黑，契合水墨调性且阅读柔和
**主色推导**: 从济南泉水的"青碧澄澈"意象出发，取青蓝相(190°-200°)中低饱和、中高明度的泉青色，既区别于普通科技蓝，又保留水的清透感；accent 向蓝偏移 15° 形成冷暖层次
**使用比例**: 65% 中性(宣纸白+墨灰) / 25% 辅助(水蓝浅底) / 10% primary(泉青)；primary 仅用于 CTA 按钮、当前导航、关键数字与品牌锚点

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(200 20% 98%) | 宣纸微暖白，页面底色 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 纯白卡片面，与背景形成微弱层次 |
| text | `--foreground` | `text-foreground` | hsl(210 25% 18%) | 深墨灰，标题正文主色 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(210 12% 45%) | 淡墨灰，辅助说明与元信息 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(192 65% 38%) | 泉青色，主交互与品牌识别 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 100%) | 纯白，primary 上的文字 |
| accent | `--accent` | `bg-accent` | hsl(195 55% 94%) | 浅水蓝，hover/选中浅底与分隔 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(192 60% 28%) | 深泉青，accent 上的文字图标 |
| border | `--border` | `border-border` | hsl(195 20% 88%) | 淡青灰边界，比普通灰更有水意 |

**语义色提示**: 成功色取青绿色系 hsl(160 45% 40%)，三态：bg `hsl(160 50% 95%)` / border `hsl(160 40% 80%)` / text `hsl(160 50% 30%)`；警告色取暖土黄 hsl(35 70% 50%)，三态：bg `hsl(40 60% 95%)` / border `hsl(40 55% 80%)` / text `hsl(30 70% 35%)`；错误色取朱砂红 hsl(5 65% 50%)，三态：bg `hsl(5 60% 96%)` / border `hsl(5 50% 82%)` / text `hsl(5 60% 40%)`；所有语义色饱和度与 primary 对齐 ±10%，保持水墨清雅基调，不出现高饱和刺眼色

## 4. 字体与节奏

- **font-display**: Noto Serif SC —— 宋体衬线气质，承载"千年文化、古城文脉"的厚重与书卷气，用于 H1/H2/板块标题与引用文字
- **font-body**: Noto Sans SC —— 清晰现代的无衬线，保障长文阅读与数字界面的信息效率，用于正文、导航、按钮与数据展示
- **字号**: H1 text-4xl md:text-5xl lg:text-6xl；H2 text-2xl md:text-3xl；H3 text-xl；body text-base；muted text-sm。
- **圆角**: 小到中 (`rounded-md`) —— 文化类不宜过度圆润，保留一点方正的书卷气；卡片与按钮统一 6-8px 圆角

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导，保留用户列出的全部板块顺序与功能模块
- **Page / Section Order**: 导航 → Hero → 核心亮点 → 文化板块 → 平台介绍 → 文化资源 → AI 智能体 → 虚拟漫游 → 时空地图 → 关于我们 → 页脚
- **Standard Content Zone**: Landing 型 `max-w-6xl` + `mx-auto`，文化内容阅读行长舒适，卡片网格舒展不拥挤
- **Shell / Frame Alignment**: 顶部导航与内容区同宽对齐，Hero / 时空地图 / 虚拟漫游等大视觉板块可全宽出血，内部文字与操作控件仍受 `max-w-6xl` 约束
- **Padding & Rhythm**: `px-4 md:px-6 lg:px-8`，板块间距 `py-16 md:py-20`，保持 8px 倍数节奏；板块之间用水墨渐变分隔线或浅水纹底过渡
- **Full-bleed Zones**: Hero 背景、时空地图容器、虚拟漫游全景区、页脚背景可 `w-full`；内部标题、按钮、说明文字受内容区约束
- **Local Narrowing**: 关于我们的联系表单、平台介绍正文可收窄至 `max-w-3xl`，提升阅读专注度
- **Overflow Strategy**: 老照片横向相册、文化资源标签行、全景场景选择器使用 `overflow-x-auto`；地图与翻书使用固定比例容器
- **Flexibility Boundary**: 允许移动端卡片列数调整、板块内边距微调、小屏字号缩放；不允许改变主色、圆角系统、字体配对与板块顺序

## 6. 视觉与动效

- **装饰**: 水墨晕染分隔线、水滴涟漪 hover、浅底水纹暗纹、印章式角标
- **阴影/边界**: 轻 —— 卡片用 `shadow-sm` + 极低透明度泉青外发光模拟水墨晕边；hover 时阴影微微扩散如水波；边界以淡青灰细线为主，减少重阴影
- **动效**: 克制雅致 —— 卡片 hover 有轻微上浮 + 涟漪扩散(box-shadow 过渡)；导航滚动时背景从透明过渡到水雾毛玻璃；平滑滚动缓动 `cubic-bezier(0.4, 0, 0.2, 1)`；标签切换用淡入淡出而非滑动；所有动效 200-300ms，不抢内容的注意力

## 7. 组件原则

- 按钮：主按钮泉青填充 + 白字，hover 略深；次按钮白底 + 泉青边框 + 泉青字，hover 浅水蓝底；文字按钮无框，hover 加下划线或浅水蓝底
- 卡片：白底 + 淡青灰细边 + 轻阴影，hover 阴影扩散 + 边框微蓝；图片卡片顶部配图，底部文字区留白充足
- 标签/Tab：下划线式或胶囊式，激活态用泉青，未激活用墨灰 + hover 浅水蓝底
- 表单：输入框白底 + 淡青灰边，focus 时边框变泉青 + 浅水蓝外发光，统一 `rounded-md`
- 加载与空状态：延续水墨调性，用淡墨灰占位 + 水纹骨架屏，不使用默认彩色 loading

## 8. Image Direction

- **Image Role**: Hero 主视觉背景图 + 文化资源卡片插图 + 虚拟漫游老照片 + 板块头图氛围
- **Image Art Direction**: 整体走"水墨摄影融合"风格——实景山水与古建筑经过低饱和、青蓝色调统一、边缘微微晕染处理，仿佛水墨与摄影的中间态；Hero 图以济南泉水、古桥、垂柳为主体，晨雾朦胧，水面有轻微涟漪反光，构图开阔留大量上方空白用于叠字；文化卡片图取对应景点的清雅视角，统一冷调色温，暗部带青，高光带暖白宣纸感；老照片板块用泛黄做旧的古籍地图质感，棕灰调带轻微纸纹
- **Image Prompt Keywords**: 济南泉水古桥垂柳晨雾, 水墨青蓝色调, 宣纸质感晕染边缘, 低饱和高对比, 东方意境留白, 水面涟漪反光, 古建筑灰瓦白墙, 古籍地图泛黄纸纹, 薄雾朦胧氛围, 冷调统一色温
- **Image Avoidance**: 避免高饱和艳色旅游宣传照, 避免卡通插画风格, 避免现代高楼大厦主体, 避免人物摆拍素材感, 避免无意义抽象渐变, 避免过度 PS 的网红滤镜感

## 9. Anti-patterns

- **Split personality**: 板块之间各自为政，有的水墨有的科技有的卡通；全站统一泉青 + 宣纸白 + 墨灰的色彩系统与 Noto Serif/Sans 字体配对
- **Default SaaS drift**: 回到默认亮蓝按钮、紫色渐变、圆角巨大的通用 SaaS 风；用泉青色、水墨晕染、宣纸质感锚定文化属性
- **Mono-hue tyranny**: 主色铺满按钮、tab、图标、边框、链接、图表；按 65-25-10 比例把 primary 收回到 CTA 与品牌锚点，其余用 accent 和中性色
- **Over-decoration**: 水墨元素堆砌，到处是山水、毛笔字、印章；装饰只出现在 Hero、分隔线、hover 反馈等关键节点，内容区保持干净可读
- **Invisible interaction**: 只做了 hover 效果，丢了 focus-visible；所有可交互元素必须有键盘可见的泉青色外发光或边框状态
- **Status color drift**: 成功警告错误用高饱和红绿黄，与清雅水墨基调冲突；语义色饱和度必须与 primary 对齐 ±10%，走低饱和东方色系